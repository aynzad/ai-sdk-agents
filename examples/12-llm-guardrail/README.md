# 12 - LLM Guardrail

LLM-as-judge guardrail using `llmGuardrail()`. A second model evaluates agent output for factuality and appropriateness, tripping the guardrail when the judge responds with "FAIL".

## Run

```bash
pnpm install
pnpm start
```

## Test

```bash
pnpm test
```
